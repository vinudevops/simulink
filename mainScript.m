function result = mainScript(inputData)
    % Your MATLAB code related to the Simulink model goes here
    % For example, perform some calculations or data processing

    % Display the input data
    disp('Input Data:');
    disp(inputData);

    % Simulate a simple operation (e.g., multiply by 2)
    result = inputData * 2;

    % Display the result
    disp('Result:');
    disp(result);
end